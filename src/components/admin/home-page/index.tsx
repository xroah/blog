import * as React from "react";

export default class HomePage extends React.Component {
    render() {
        return (
            <section>
                主页
            </section>
        );
    }
}