import * as React from "react";

export default class Classification extends React.Component {
    render() {
        return (
            <section>
                分类
            </section>
        );
    }
}