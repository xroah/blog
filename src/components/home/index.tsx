import * as React from "react";
import Nav from "../nav";

export default class Home extends React.Component {
    render() {
        return (
            <section>
                <Nav/>
                <div>home</div>
            </section>
        );
    }
}